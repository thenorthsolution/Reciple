{
  "name": "reciple",
  "license": "GPL-3.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "npx rimraf ./packages/bin ./packages/dist",
    "build": "npm run clean && npx lerna run build && npm i"
  },
  "devDependencies": {
    "lerna": "^6.1.0",
    "prettier": "^2.8.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.4"
  }
}
