{
  "name": "@reciple/monorepo",
  "license": "GPL-3.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "example"
  ],
  "scripts": {
    "clean": "npx rimraf ./packages/bin ./packages/dist ./node_modules/.bin",
    "build": "npx lerna run build",
    "watch": "npx tsc --watch --noEmit",
    "test": "npx lerna run test",
    "publish:dev": "npx lerna publish prerelease",
    "docs": "npx lerna run docs"
  },
  "devDependencies": {
    "@discordjs/docgen": "^0.12.1",
    "@types/node": "^20.4.2",
    "lerna": "^7.1.4",
    "rimraf": "^5.0.1",
    "ts-mixer": "^6.0.3",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "globby": "^13.2.2",
    "reciple": "^7.9.0"
  }
}
