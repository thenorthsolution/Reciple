{
  "name": "reciple",
  "version": "1.0.15",
  "description": "A Discord.js bot",
  "author": "FalloutStudios",
  "license": "GPL-3.0",
  "main": "bin/index.js",
  "bin": {
    "reciple": "bin/bin.js"
  },
  "files": [
    "bin",
    "resource",
    "package.json",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "clean": "rimraf bin",
    "compile": "yarn run clean && tsc",
    "build": "yarn run compile && yarn un reciple -g && yarn i ./ -g",
    "build:publish": "yarn run build && yarn publish",
    "test": "cd test && reciple",
    "test:compile": "yarn run build && yarn run test:run"
  },
  "dependencies": {
    "commander": "^9.2.0",
    "discord.js": "^13.6.0",
    "dotenv": "^16.0.0",
    "fallout-utility": "^1.3.13",
    "yaml": "^2.0.1"
  },
  "devDependencies": {
    "@types/node": "^17.0.24",
    "typescript": "^4.6.3"
  }
}
