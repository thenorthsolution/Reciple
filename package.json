{
  "name": "reciple-repo",
  "license": "GPL-3.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "npx rimraf ./packages/bin ./packages/dist",
    "build": "npm run clean && npx lerna run build && npm i"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "lerna": "^6.1.0",
    "rimraf": "^3.0.2",
    "ts-mixer": "^6.0.2",
    "typescript": "^4.9.4"
  }
}
