{
  "name": "reciple",
  "version": "1.0.0",
  "description": "A Discord.js bot",
  "author": "FalloutStudios",
  "license": "GPL-3.0",
  "main": "bin/index.js",
  "bin": {
    "reciple": "bin/bin.js"
  },
  "scripts": {
    "clean": "rimraf bin",
    "compile": "npm run clean && tsc",
    "build": "npm run compile && npm un reciple -g && npm i ./ -g",
    "test:run": "cd test && reciple",
    "test:compile": "npm run build && npm run test:run"
  },
  "dependencies": {
    "commander": "^9.1.0",
    "discord.js": "^13.6.0",
    "fallout-utility": "^1.3.9",
    "yaml": "^1.10.2"
  },
  "devDependencies": {
    "@types/node": "^17.0.23",
    "typescript": "^4.6.3"
  }
}
